'use strict';

const Resources = require('../../net/Resources');
const resourceMethod = Resources.method;

module.exports = Resources.extend({
  path: '/v1/charges',
  includeBasic: ['create', 'retrieve', 'list'],

  capture: resourceMethod({
    method: 'POST',
    path: '/{id}/capture',
  }),

});
